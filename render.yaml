services:
  # Django Backend Service
  - type: web
    name: healthbridge360-backend
    runtime: python
    plan: free
    buildCommand: "pip install setuptools==74.1.2 wheel && pip install --upgrade pip setuptools==74.1.2 && pip install -r requirements.txt"
    startCommand: "python manage.py migrate && python manage.py create_superuser && python manage.py collectstatic --noinput --clear && gunicorn healthkart360.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 2"
    envVars:
      - key: DEBUG
        value: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: healthkart360-db
          property: connectionString
      - key: ALLOWED_HOSTS
        fromService:
          type: web
          name: healthbridge360-backend
          property: host
      - key: RAZORPAY_KEY_ID
        value: placeholder_razorpay_key_id
      - key: RAZORPAY_KEY_SECRET
        value: placeholder_razorpay_key_secret
      - key: SENDGRID_API_KEY
        value: placeholder_sendgrid_api_key
      - key: DEFAULT_FROM_EMAIL
        value: noreply@healthbridge360.com
      - key: SECURE_SSL_REDIRECT
        value: true
      - key: SITE_URL
        fromService:
          type: web
          name: healthbridge360-backend
          property: host
      - key: RATE_LIMIT_REQUESTS
        value: 100
      - key: RATE_LIMIT_WINDOW
        value: 3600

  # Celery Worker Service
  - type: worker
    name: healthbridge360-celery-worker
    runtime: python
    plan: free
    buildCommand: "pip install setuptools==74.1.2 wheel && pip install --upgrade pip setuptools==74.1.2 && pip install -r requirements.txt"
    startCommand: "celery -A healthkart360 worker --loglevel=info"
    envVars:
      - key: DEBUG
        value: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: healthkart360-db
          property: connectionString
      - key: ALLOWED_HOSTS
        fromService:
          type: web
          name: healthbridge360-backend
          property: host
      - key: RAZORPAY_KEY_ID
        value: placeholder_razorpay_key_id
      - key: RAZORPAY_KEY_SECRET
        value: placeholder_razorpay_key_secret
      - key: SENDGRID_API_KEY
        value: placeholder_sendgrid_api_key
      - key: DEFAULT_FROM_EMAIL
        value: noreply@healthbridge360.com
      - key: SECURE_SSL_REDIRECT
        value: true
      - key: SITE_URL
        fromService:
          type: web
          name: healthbridge360-backend
          property: host
      - key: RATE_LIMIT_REQUESTS
        value: 100
      - key: RATE_LIMIT_WINDOW
        value: 3600
      - key: DJANGO_SUPERUSER_USERNAME
        generateValue: true
      - key: DJANGO_SUPERUSER_EMAIL
        generateValue: true
      - key: DJANGO_SUPERUSER_PASSWORD
        generateValue: true
      - key: REDIS_URL
        value: redis://localhost:6379/0

  # Next.js Frontend Service
  - type: web
    name: healthkart360-frontend
    runtime: node
    plan: free
    buildCommand: "pnpm install && pnpm build"
    startCommand: "pnpm start"
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: healthbridge360-backend
          property: host

# PostgreSQL Database
databases:
  - name: healthkart360-db
    databaseName: healthkart360
    user: healthkart360_user
    plan: free
